# メールユーザ管理システム (Mail User Management)

**バージョン**: 1.0.0 (開発中)
**更新日**: 2025-11-05
**ステータス**: 🚧 開発中 - Phase 1 (ドキュメント整備完了)

---

## 📋 概要

メールサーバーのユーザアカウントを Web インターフェースから管理するための Flask ベースの管理画面です。

**主要機能**:
- ✉️ メールユーザの追加・編集・削除 (CRUD操作)
- 🏢 ドメイン別のユーザ管理・グルーピング表示
- 🔐 パスワード設定・変更 (Dovecot互換SHA512-CRYPT)
- 💾 メールボックス容量(quota)設定
- 📊 ダッシュボード（ドメイン一覧、ユーザ統計）
- 🔒 Tailscale VPN 内部アクセス専用のセキュリティ

**技術スタック**:
- **Backend**: Flask 3.0, SQLAlchemy 2.0, Python 3.11+
- **Database**: MariaDB 10.11 (既存の `mailserver-mariadb` コンテナ利用)
- **Frontend**: Bootstrap 5.3, Jinja2 テンプレート
- **Security**: Flask-Login, Flask-WTF (CSRF保護), Tailscale VPN認証
- **Deployment**: Docker + Gunicorn

---

## 🏗️ アーキテクチャ

```
┌─────────────────────────────────────────────────────────┐
│           Tailscale VPN ネットワーク (100.x.x.x/10)      │
│                                                          │
│  管理者デバイス (PC/Mac/iPhone/Android)                  │
│         │ HTTPS (443)                                    │
│         ▼                                                │
│  ┌─────────────────┐                                    │
│  │  Nginx Reverse  │ /admin パス                         │
│  │  Proxy          │ → usermgmt コンテナへプロキシ        │
│  └────────┬────────┘                                    │
│           │                                              │
│           ▼                                              │
│  ┌─────────────────┐                                    │
│  │  usermgmt       │ Flask + Gunicorn                    │
│  │  172.20.0.90    │ Port 5000                           │
│  └────────┬────────┘                                    │
│           │                                              │
│           ▼                                              │
│  ┌─────────────────┐                                    │
│  │  MariaDB        │ mailserver_usermgmt データベース    │
│  │  172.20.0.60    │ users, domains, audit_logs テーブル │
│  └─────────────────┘                                    │
└─────────────────────────────────────────────────────────┘
```

**認証フロー**:
1. Dovecot File認証 (`/etc/dovecot/users`) - 既存ユーザとの互換性維持
2. Dovecot SQL認証 (MariaDB) - 新規ユーザ管理方式
3. 両方の認証方式が並行稼働（段階的移行）

---

## 🚀 クイックスタート

### 前提条件

- Docker および Docker Compose がインストール済み
- 既存の mailserver 環境が稼働中
- Tailscale VPN 接続済み

### セットアップ手順

#### 1. 環境変数設定

`.env` ファイルに以下を追加:

```bash
# User Management Application
USERMGMT_DB_PASSWORD=<secure-password>
USERMGMT_ADMIN_USERNAME=admin
USERMGMT_ADMIN_PASSWORD_HASH=<sha512-crypt-hash>
USERMGMT_SECRET_KEY=<random-secret-key>
```

**パスワードハッシュ生成**:
```bash
python3 -c "from passlib.hash import sha512_crypt; print('{SHA512-CRYPT}' + sha512_crypt.hash('your-password'))"
```

**SECRET_KEY生成**:
```bash
openssl rand -hex 32
```

#### 2. MariaDB データベース初期化

```bash
# MariaDB コンテナに接続
docker exec -it mailserver-mariadb mysql -u root -p

# データベースとユーザ作成
CREATE DATABASE mailserver_usermgmt CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'usermgmt'@'%' IDENTIFIED BY '<USERMGMT_DB_PASSWORD>';
GRANT ALL PRIVILEGES ON mailserver_usermgmt.* TO 'usermgmt'@'%';
FLUSH PRIVILEGES;

# テーブル作成スクリプト実行
USE mailserver_usermgmt;
SOURCE /path/to/schema.sql;
```

#### 3. Docker コンテナ起動

```bash
cd /opt/onprem-infra-system/project-root-infra/services/mailserver

# usermgmt コンテナをビルド
docker-compose build usermgmt

# usermgmt コンテナを起動
docker-compose up -d usermgmt

# ヘルスチェック確認
docker logs mailserver-usermgmt
curl http://172.20.0.90:5000/health
```

#### 4. Nginx 統合 (オプション)

Nginx設定に `/admin` パスを追加して、Tailscale VPN経由でアクセス可能にします。

```bash
# Nginx 設定ファイル編集
vi config/nginx/templates/mailserver.conf.template

# Nginx reload
docker exec mailserver-nginx nginx -t
docker exec mailserver-nginx nginx -s reload
```

#### 5. アクセス

Tailscale VPN 接続後、以下の URL にアクセス:

```
https://dell-workstation.tail67811d.ts.net/admin
```

管理者ユーザ名とパスワードでログインしてください。

---

## 📂 プロジェクト構造

```
services/mailserver/usermgmt/
├── app/
│   ├── __init__.py          # Flask アプリ初期化
│   ├── models/              # SQLAlchemy モデル
│   │   ├── __init__.py
│   │   ├── user.py          # User モデル
│   │   ├── domain.py        # Domain モデル
│   │   └── audit_log.py     # AuditLog モデル
│   ├── routes/              # Flask ルート
│   │   ├── __init__.py
│   │   ├── auth.py          # 認証ルート (login/logout)
│   │   ├── users.py         # ユーザ管理ルート
│   │   └── domains.py       # ドメイン管理ルート
│   └── services/            # ビジネスロジック
│       ├── __init__.py
│       ├── database.py      # DB接続管理
│       ├── user_service.py  # ユーザ操作サービス
│       └── password.py      # パスワードハッシュ化
├── templates/               # Jinja2 テンプレート
│   ├── base.html            # ベーステンプレート
│   ├── login.html           # ログイン画面
│   ├── dashboard.html       # ダッシュボード
│   ├── users/
│   │   ├── list.html        # ユーザ一覧
│   │   ├── create.html      # ユーザ作成フォーム
│   │   └── edit.html        # ユーザ編集フォーム
│   └── domains/
│       └── list.html        # ドメイン一覧
├── static/                  # CSS/JS/画像
│   ├── css/
│   │   └── custom.css       # カスタムスタイル
│   └── js/
│       └── validation.js    # フォームバリデーション
├── config/                  # 設定ファイル
│   └── config.py            # Flask 設定
├── scripts/                 # 運用スクリプト
│   ├── init_db.py           # DB初期化
│   └── migrate_users.py     # 既存ユーザ移行
├── tests/                   # テストコード
│   ├── test_models.py
│   ├── test_routes.py
│   └── test_services.py
├── app.py                   # アプリケーションエントリポイント
├── requirements.txt         # Python 依存関係
├── Dockerfile               # Docker イメージ定義
├── .env.example             # 環境変数サンプル
├── README.md                # 本ファイル
├── DEVELOPMENT.md           # 開発進捗トラッキング
├── API.md                   # API エンドポイント仕様
└── CHANGELOG.md             # 変更履歴
```

---

## 🔐 セキュリティ

### アクセス制御

- **Tailscale VPN 必須**: `100.0.0.0/10` 範囲からのアクセスのみ許可
- **HTTPS 強制**: HTTP アクセスは自動的に HTTPS にリダイレクト
- **認証必須**: すべての管理画面は Flask-Login による認証が必須
- **CSRF 保護**: Flask-WTF による CSRF トークン検証

### パスワードセキュリティ

- **ハッシュ方式**: SHA512-CRYPT (Dovecot 互換)
- **パスワードポリシー**:
  - 最小長: 8文字
  - 複雑性: 英大文字、英小文字、数字、記号のうち3種類以上
- **平文保存禁止**: データベースには必ずハッシュ化されたパスワードのみ保存

### 監査ログ

すべてのユーザ管理操作（作成・編集・削除・パスワード変更）は `audit_logs` テーブルに記録されます。

---

## 🛠️ 開発

### 開発環境セットアップ

```bash
# Python 仮想環境作成
python3 -m venv venv
source venv/bin/activate

# 依存関係インストール
pip install -r requirements.txt

# 開発サーバー起動
export FLASK_ENV=development
export FLASK_APP=app.py
flask run --host=0.0.0.0 --port=5000
```

### テスト実行

```bash
# ユニットテスト
pytest tests/

# カバレッジレポート
pytest --cov=app tests/
```

### 開発進捗の確認

開発の進捗状況は `DEVELOPMENT.md` で管理しています。

---

## 📚 関連ドキュメント

- **設計書**: `../../Docs/application/mailserver/05_user_management_design.md`
- **API仕様**: `API.md`
- **開発進捗**: `DEVELOPMENT.md`
- **変更履歴**: `CHANGELOG.md`
- **Mailserver 全体設計**: `../../Docs/application/mailserver/README.md`

---

## 🐛 トラブルシューティング

### コンテナが起動しない

```bash
# ログ確認
docker logs mailserver-usermgmt

# MariaDB 接続テスト
docker exec mailserver-usermgmt python3 -c "
from app.services.database import db_engine
print('DB connection OK' if db_engine else 'DB connection failed')
"
```

### ログインできない

- `.env` ファイルの `USERMGMT_ADMIN_PASSWORD_HASH` が正しく設定されているか確認
- パスワードハッシュは `{SHA512-CRYPT}` プレフィックスが必要

### Tailscale VPN 経由でアクセスできない

- Nginx の geo ディレクティブで `100.0.0.0/10` が許可されているか確認
- Tailscale 接続ステータスを確認: `tailscale status`

---

## 📝 ライセンス

このプロジェクトは内部利用専用です。

---

## 👥 メンテナ

- システム管理者 (system-admin@kuma8088.com)

---

## 🔗 リンク

- **プロジェクトルート**: `/opt/onprem-infra-system/project-root-infra`
- **Mailserver サービス**: `services/mailserver/`
- **ドキュメント**: `Docs/application/mailserver/`
